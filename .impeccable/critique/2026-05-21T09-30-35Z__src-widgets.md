---
target: Hero, Newsletter, PromoBanner, ProductGrid
total_score: 26
p0_count: 1
p1_count: 3
timestamp: 2026-05-21T09-30-35Z
slug: src-widgets
---
#### Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Good skeletons for loading, but error states are visually disjointed and form lacks pending states |
| 2 | Match System / Real World | 3 | Natural language, but the countdown is completely static and fake |
| 3 | User Control and Freedom | 3 | Basic links work, but quick add-to-cart is blocked with a toast warning |
| 4 | Consistency and Standards | 1 | Severe bypass of UI library components (Button, Input) and design tokens (rounded-[4px], rounded-[32px]) |
| 5 | Error Prevention | 3 | ProductGrid catches fetch errors, but the container has hardcoded aesthetics |
| 6 | Recognition Rather Than Recall | 4 | Clean product grid presentation and standard layouts |
| 7 | Flexibility and Efficiency | 2 | Add-to-cart block forces a click-through for every item; no keyboard accelerators |
| 8 | Aesthetic and Minimalist Design | 2 | Hardcoded colors, glassmorphism on list containers, and rigid text size sizing |
| 9 | Error Recovery | 2 | No retry or reload actions provided on the error state page |
| 10 | Help and Documentation | 3 | Basic labels exist, but no contextual help links or tooltips |
| **Total** | | **26/40** | **Acceptable (Significant improvements needed)** |

#### Anti-Patterns Verdict

**Start here.** Does this look AI-generated?

**LLM assessment**: The visual composition and layout of the widgets follow standard minimalist aesthetics, but the execution suffers from significant technical debt and inconsistency. There is a frequent bypass of standard project-wide UI components (`Button`, `Input`) in favor of manual Tailwind inline implementations. Furthermore, the use of hardcoded colors, mismatched border-radius values, and a local filesystem image path are strong indicators of incomplete code generation.

**Deterministic scan**: Deterministic scan unavailable (bundled detector not found).

**Visual overlays**: No reliable user-visible overlay is available because browser automation is not supported in this session.

#### Overall Impression
The widgets possess a clean layout and use typography well, but they violate critical design tokens and the project's design system rules. Bypassing the shared UI library creates severe inconsistency, and hardcoded local image paths and colors will cause visual/technical breakages in production.

#### What's Working
1. **Responsive Composition**: The `ProductGrid` and `Hero` use robust grid and flex layouts that adjust cleanly to mobile viewports.
2. **Harmonious Typography**: The pairing of the serif typeface for headings and sans-serif for body text creates an editorial, premium look in line with the brand identity.

#### Priority Issues

- **[P0] Hero Image Path**: The image `src` in [Hero.tsx](file:///d:/Hutech/DACS/ECOMMERCE_PROJECT/ECommerce_FE/src/widgets/Hero/Hero.tsx#L47) is a hardcoded absolute Windows path (`/C:/Users/...`), which will break on all non-local machines.
  - *Fix*: Replace with a relative asset path (e.g. `/images/hero-shoe.png`) or a public URL.
  - *Suggested command*: `manual edit`
- **[P1] Bypassed Shared UI Components**: Standard `Button` and `Input` components from `@shared/ui` are bypassed in [Hero.tsx](file:///d:/Hutech/DACS/ECOMMERCE_PROJECT/ECommerce_FE/src/widgets/Hero/Hero.tsx#L31-L40), [Newsletter.tsx](file:///d:/Hutech/DACS/ECOMMERCE_PROJECT/ECommerce_FE/src/widgets/Newsletter/Newsletter.tsx#L20-L33), and [PromoBanner.tsx](file:///d:/Hutech/DACS/ECOMMERCE_PROJECT/ECommerce_FE/src/widgets/Promo/PromoBanner.tsx#L48-L62).
  - *Fix*: Import and use the custom `<Button>` and `<Input>` components.
  - *Suggested command*: `manual edit` or `/impeccable polish`
- **[P1] Hardcoded Colors and Forbidden Glassmorphism in ProductGrid**: [ProductGrid.tsx](file:///d:/Hutech/DACS/ECOMMERCE_PROJECT/ECommerce_FE/src/widgets/Product/ProductGrid.tsx#L55-L77) uses hardcoded hex colors (`#11120D`, `#8B4513`, `#565449`) and an unauthorized glass effect (`bg-white/50 backdrop-blur-md`) on static message blocks, violating dark mode and design system principles.
  - *Fix*: Replace colors with tailwind classes using design tokens (`text-error`, `text-muted-foreground`), and use standard solid panels or the `.glass-panel` class.
  - *Suggested command*: `/impeccable polish`
- **[P1] Fake Countdown Timer**: [PromoBanner.tsx](file:///d:/Hutech/DACS/ECOMMERCE_PROJECT/ECommerce_FE/src/widgets/Promo/PromoBanner.tsx#L27-L46) uses a static array that never ticks down, violating the principle of avoiding "countdown timer giả tạo".
  - *Fix*: Implement a ticking countdown state hook or replace it with a clear end-date string.
  - *Suggested command*: `/impeccable polish`
- **[P2] Non-Standard Border Radii**: Components use `rounded-[4px]`, `rounded-[8px]`, and `rounded-[32px]` instead of the design tokens `rounded-button`, `rounded-card`, and `rounded-2xl`.
  - *Fix*: Swap inline border radius utilities with standard CSS config tokens.
  - *Suggested command*: `/impeccable polish`

#### Persona Red Flags

- **Alex (Impatient Power User)**: Quick Add to Cart button in `ProductGrid` triggers a toast informing the user to view details rather than adding the item, introducing a mandatory, friction-heavy extra click.
- **Jordan (First-Timer)**: The email input in `Newsletter` lacks an associated `<label>` or `aria-label`, making it inaccessible to screen readers.
- **Casey (Distracted Mobile User)**: Huge font sizes (`text-[180px]`) in the discount text of `PromoBanner` risk layout overflow on smaller viewports.

#### Minor Observations
- The divider line in `ProductGrid` uses `bg-surface` which matches the background color in dark mode, making the divider invisible. It should use `bg-border` or `.section-divider`.
- The newsletter value text "We value your privacy..." uses a custom size `text-[10px]` instead of the token standard `text-xs`.

#### Questions to Consider
- "Should the quick add-to-cart action fetch the default variant (e.g. first size/color) and add it immediately to avoid blocking the user with a toast?"
- "Can we refactor the PromoBanner countdown to use a simple client-side React hook timer, or change it to a static text promo code?"
- "Would it be better to extract the empty/error state layouts into a single reusable placeholder widget to keep all states consistent across the app?"
